simple TCP server and client using tornado
=========================================

Tested with Python3.5, Tornado4.3.

Run the server first, then the client:
```shell
python server.py

python client.py
```
